package com.kodilla.good.patterns.good.patterns.spring.intro.exception.main;

import com.kodilla.good.patterns.good.patterns.spring.intro.exception.io.FileReader;

public class ExceptionModuleRunner {

    public static void main(String args[]) {


        FileReader fileReader = new FileReader();
        //File file = new File(classLoader.getResource("file/names.txt").getFile());
        //try (Stream<String> fileLines = Files.lines(Path.get(file.getPath()))) {
         //   ileLines.forEach(System.out::println);
        //} catch (IOException e) {
        //    e.printStackTrace();
        //}

    }
}
